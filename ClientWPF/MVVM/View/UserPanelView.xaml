<UserControl x:Class="ClientWPF.MVVM.View.UserPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ClientWPF.MVVM.View"
             mc:Ignorable="d"
             d:DesignHeight="440" d:DesignWidth="250">
    <Grid>
        <StackPanel Background="#28292A">
            <Label Height="30"
                   Content="Enter private LUC key"
                   FontSize="14"
                   FontWeight="Bold"
                   Foreground="LightGray"
                   Margin="15, 10, 0,0" />
            <Label Height="25"
                   Content="Private LUC key"
                   FontSize="12"
                   FontWeight="Bold"
                   Foreground="Gray"
                   Margin="15, 0, 0,0" />
            <TextBox Width="220"
                     Height="30"
                     VerticalContentAlignment="Center"
                     HorizontalAlignment="Left"
                     Margin="15, 5, 0,5"
                     Text="{Binding PrivateLUCkey}"
                     Style="{StaticResource TextBoxLucPrKeyTheme}" />
            <Label Height="25"
                   Content="N"
                   FontSize="12"
                   FontWeight="Bold"
                   Foreground="Gray"
                   Margin="15, 0, 0,0" />
            <TextBox Width="220"
                     Height="30"
                     VerticalContentAlignment="Center"
                     HorizontalAlignment="Left"
                     Margin="15, 5, 0,5"
                     Text="{Binding NLUCkey}"
                     Style="{StaticResource TextBoxNTheme}" />
            <Button Width="220"
                    Margin="15, 10"
                    Content="Get DEAL key from server"
                    Background="#252525"
                    BorderBrush="#252525"
                    Foreground="LightGray"
                    FontSize="14" 
                    Command="{Binding GetDEALkeyCommand}"/>
            <TextBox Width="220"
                     Height="30"
                     VerticalContentAlignment="Center"
                     HorizontalAlignment="Left"
                     Margin="15, 5, 0,10"
                     Text="{Binding DEALkey}"
                     Style="{StaticResource TextBoxDealKeyTheme}" />
        </StackPanel>
    </Grid>
</UserControl>